---
phase: 05-client-utm-capture-hook
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - client/src/App.tsx
  - client/src/components/LeadFormModal.tsx
autonomous: true
requirements: [SESSION-01, SESSION-03]

must_haves:
  truths:
    - "On any page load with `?utm_source=X` in the URL, a `visitor_sessions` row is created server-side with the correct UTM values — even after Wouter navigation."
    - "When no UTM params are present, `document.referrer` is classified into Organic Search / Social / Referral / Direct, and `visitor_sessions.ft_source_channel` reflects that."
    - "`localStorage` key `mvp_vid` persists across reloads and is the same UUID sent on every session ping."
    - "Submitting the lead form passes `visitorId` so the server can stamp `form_leads.visitor_id` (verifiable by inserting a test row and checking the column)."
    - "Page-view hits with the visitorId are sent to `/api/analytics/hit` on every Wouter navigation, populating `analytics_event_hits.visitor_id` for the journey view."
  artifacts:
    - path: "client/src/App.tsx"
      provides: "useAttribution() called once inside AnalyticsProvider; reportAttributionPageView fired alongside trackPageView in [location] effect; visitorId state available to children if needed in future"
      contains: "useAttribution"
    - path: "client/src/components/LeadFormModal.tsx"
      provides: "visitorId added to the /api/form-leads/progress payload"
      contains: "visitorId"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/hooks/use-attribution.ts"
      via: "import { useAttribution } from '@/hooks/use-attribution'"
      pattern: "from ['\"]@/hooks/use-attribution['\"]"
    - from: "client/src/App.tsx"
      to: "client/src/lib/attribution.ts"
      via: "import { reportAttributionPageView } from '@/lib/attribution' (called in [location] effect)"
      pattern: "reportAttributionPageView"
    - from: "client/src/components/LeadFormModal.tsx"
      to: "client/src/lib/attribution.ts"
      via: "import { getStoredVisitorId } from '@/lib/attribution'; payload.visitorId = getStoredVisitorId()"
      pattern: "getStoredVisitorId"
---

<objective>
Wire the foundation from Plan 01 into the React tree so the attribution pipeline becomes live.

Purpose: Without these wires, `useAttribution()` never runs, no session pings are sent, no `visitor_sessions` rows are created, and no `form_leads.visitor_id` is populated. SESSION-01 and SESSION-03 require this wiring; the page-view fan-out (CONV-05 already shipped server-side in Phase 4) needs `visitorId` to land in `analytics_event_hits` for the journey view.

Output:
- `client/src/App.tsx` — `useAttribution()` called once inside `AnalyticsProvider` (the existing `AppContent`-equivalent component that already owns the `useLocation()` + `useEffect([location])` analytics hook). Same `[location]` effect now also fires `reportAttributionPageView`.
- `client/src/components/LeadFormModal.tsx` — `visitorId` added to the `payload` object built before the `fetch("/api/form-leads/progress", ...)` call near line 485.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-utm-capture-hook/05-CONTEXT.md
@.planning/phases/05-client-utm-capture-hook/05-01-SUMMARY.md
@client/src/App.tsx
@client/src/components/LeadFormModal.tsx
@client/src/lib/attribution.ts
@client/src/hooks/use-attribution.ts

<interfaces>
<!-- From Plan 01 (now on disk). Use these exactly. -->

From client/src/lib/attribution.ts:
```typescript
export const MVP_VID_KEY: 'mvp_vid';
export function getStoredVisitorId(): string | null;
export function reportAttributionPageView(pagePath: string, visitorId: string | null): void;
// (other helpers used elsewhere — not needed in this plan)
```

From client/src/hooks/use-attribution.ts:
```typescript
export function useAttribution(): { visitorId: string | null };
```
- Must be called ONCE at App root. Calling it in route components or page components causes UTM capture to re-fire and read empty `window.location.search` after Wouter navigation (per D-02).

Existing AnalyticsProvider in client/src/App.tsx (lines 129-201) — this is the correct mount point. It already:
- Has `const [location] = useLocation();` (line 133)
- Has `useEffect(() => { trackPageView(location); }, [location]);` (lines 149-151)
- Has access to React Query + Theme but NOT to a parent App-level state we need to share.

Existing payload build in client/src/components/LeadFormModal.tsx (lines ~430-489):
- An object literal `payload` is built incrementally (`if (effectiveAnswers.nome) payload.nome = ...`).
- The fetch call is at line 485: `fetch("/api/form-leads/progress", { method: "POST", ..., body: JSON.stringify(payload) })`.
- Server already accepts `visitorId` as optional in `formLeadProgressSchema` (Phase 4 plan 01 added this — see STATE.md decisions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire useAttribution into App.tsx + add reportAttributionPageView to the [location] effect</name>
  <files>client/src/App.tsx</files>
  <read_first>
    - client/src/App.tsx (the WHOLE file — must not break the existing AnalyticsProvider, SEOProvider, or Router structure; lines 129-201 are the surgical zone)
    - client/src/hooks/use-attribution.ts (just shipped by Plan 01 — confirm export name + return shape)
    - client/src/lib/attribution.ts (just shipped by Plan 01 — confirm `reportAttributionPageView` signature)
    - .planning/phases/05-client-utm-capture-hook/05-CONTEXT.md (D-02, D-15, D-16)
  </read_first>
  <action>
Modify `client/src/App.tsx` with TWO surgical edits inside the existing `AnalyticsProvider` function (lines 129-201). Do NOT touch any other component, the route table, the Router function, the InitialLoadContext, or the color-token logic.

**Edit 1 — Imports (top of file):**

Add two new imports after the existing `import { initAnalytics, trackPageView } from "@/lib/analytics";` line (currently line 11):

```typescript
import { useAttribution } from "@/hooks/use-attribution";
import { reportAttributionPageView } from "@/lib/attribution";
```

Keep imports alphabetized within their groups if the file has that convention (it does not — it's grouped by source). Place them after the analytics import to group with the other tracking imports.

**Edit 2 — Inside AnalyticsProvider (line 129-201):**

Locate the existing two leading hooks at lines 130-133:
```typescript
function AnalyticsProvider({ children }: { children: React.ReactNode }) {
  const { data: settings } = useQuery<CompanySettings>({
    queryKey: ['/api/company-settings'],
  });
  const [location] = useLocation();
```

IMMEDIATELY after `const [location] = useLocation();` and BEFORE `const { resolvedTheme } = useTheme();`, add:

```typescript
  const { visitorId } = useAttribution();
```

This places the hook call at the App root per D-02 (AnalyticsProvider is wrapped by SEOProvider → AuthProvider → TooltipProvider → QueryClientProvider → ThemeProvider → InitialLoadContext.Provider, so it mounts once for the whole app and never remounts on route changes).

Then update the existing page-view effect (lines 149-151):

BEFORE:
```typescript
  useEffect(() => {
    trackPageView(location);
  }, [location]);
```

AFTER:
```typescript
  useEffect(() => {
    trackPageView(location);
    reportAttributionPageView(location, visitorId);
  }, [location, visitorId]);
```

Why add `visitorId` to the deps array: when `useAttribution`'s Effect A runs and calls `setVisitorId(id)`, this effect re-fires once and emits the first page_view with a known visitor. Without `visitorId` in deps, the very first navigation may fire before `visitorId` resolves and `reportAttributionPageView` would no-op (it returns early when visitorId is null) — which is acceptable since Plan 01's hook also fires the first page_view in Effect A, but the dependency keeps `react-hooks/exhaustive-deps` quiet and is semantically correct.

Do NOT:
- Add `page_view` to `SERVER_REPORTED_EVENTS` in `client/src/lib/analytics.ts` (per D-16 — keep attribution page_view separate from the generic analytics fan-out).
- Call `useAttribution` in any route component or page component (per D-02 — only at App root).
- Pass `visitorId` to `Router`, `Navbar`, or any child component via context or prop in this plan. Plan 03 reads `mvp_vid` directly from localStorage via `getStoredVisitorId()` — no prop drilling needed.
- Change the AnalyticsProvider position in the Provider stack (line 284 — `<AnalyticsProvider>` is correctly nested inside `<SEOProvider>` inside `<AuthProvider>` inside `<QueryClientProvider>`; do not move it).

After the edit, the AnalyticsProvider function should have these hooks in this order:
1. `useQuery<CompanySettings>` for settings
2. `useLocation()` for location
3. `useAttribution()` for visitorId  ← NEW
4. `useTheme()` for resolvedTheme
5. `useEffect(initAnalytics, [settings])`
6. `useEffect(trackPageView + reportAttributionPageView, [location, visitorId])`  ← UPDATED
7. `useEffect(color token plumbing, [location, settings, resolvedTheme])`
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from ['\"]@/hooks/use-attribution['\"]" client/src/App.tsx` returns exactly 1 match.
    - `grep -n "from ['\"]@/lib/attribution['\"]" client/src/App.tsx` returns exactly 1 match.
    - `grep -n "useAttribution()" client/src/App.tsx` returns exactly 1 match (single call site at App root per D-02).
    - `grep -n "reportAttributionPageView(location, visitorId)" client/src/App.tsx` returns exactly 1 match — must be called from inside the `[location, visitorId]` effect, NOT inside `initAnalytics` effect.
    - `grep -n "}, \\[location, visitorId\\]" client/src/App.tsx` returns at least 1 match (correct deps array on the page-view effect).
    - `grep -nE "trackPageView\\(location\\)" client/src/App.tsx` still returns 1 match (existing analytics call PRESERVED, not replaced).
    - `grep -n "useAttribution" client/src/pages/" 2>/dev/null` (and `client/src/components/`) returns NO matches except for the LeadFormModal change in Task 2 (Task 2 uses `getStoredVisitorId`, not the hook — so Pages must have ZERO `useAttribution` references).
    - `grep -n "page_view" client/src/lib/analytics.ts` returns no NEW additions to `SERVER_REPORTED_EVENTS` (per D-16).
    - `npm run check` passes.
  </acceptance_criteria>
  <done>
    AnalyticsProvider in App.tsx now calls `useAttribution()` exactly once and fires `reportAttributionPageView(location, visitorId)` alongside `trackPageView(location)` on every navigation. The wiring is invisible to all other components — they continue to work as before.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Inject visitorId into the LeadFormModal payload</name>
  <files>client/src/components/LeadFormModal.tsx</files>
  <read_first>
    - client/src/components/LeadFormModal.tsx (lines 1-30 for imports, 430-505 for the payload-build region — do NOT need to read the full ~700 line file)
    - client/src/lib/attribution.ts (confirm `getStoredVisitorId` signature — returns `string | null`)
    - .planning/phases/05-client-utm-capture-hook/05-CONTEXT.md (D-13, D-14)
    - shared/schema.ts (formLeadProgressSchema accepts visitorId as optional — Phase 4 plan 01 added this)
  </read_first>
  <action>
Modify `client/src/components/LeadFormModal.tsx`. The change is small (one import + one payload-field assignment).

**Edit 1 — Add import:**

Near the top of the file with the other `@/lib/...` imports, add:
```typescript
import { getStoredVisitorId } from "@/lib/attribution";
```

**Edit 2 — Inject visitorId into the payload:**

Locate the payload-build region around lines 430-489. The pattern is a series of `if (effectiveAnswers.X) payload.X = ...;` lines, then a `try { const res = await fetch("/api/form-leads/progress", ...) }` block at line 484.

IMMEDIATELY BEFORE the `try {` block (around line 484, after the last `if (effectiveAnswers.expectativaResultado) payload.expectativaResultado = ...;` line), add:

```typescript
      // Attach the persisted visitor ID so the server can link this lead to its visitor_sessions row.
      // Per D-13/D-14: visitorId is optional; if useAttribution hasn't resolved or localStorage is unavailable,
      // we send the form without it — server treats `visitorId` as optional in formLeadProgressSchema.
      const storedVisitorId = getStoredVisitorId();
      if (storedVisitorId) {
        payload.visitorId = storedVisitorId;
      }
```

Why use `getStoredVisitorId` instead of `useAttribution()`:
- Per D-13/D-18: the utility lookup avoids re-mounting the hook in a deeply nested form component, avoids any timing race with the App-root hook's mount effect (the `mvp_vid` key is written synchronously in `ensureVisitorId` BEFORE the first ping fires), and matches the booking-CTA wiring approach in Plan 03 for consistency.

Type concern: the `payload` variable is likely typed as a `Partial<FormLeadProgress>` or `Record<string, unknown>` (read the file to confirm — DO NOT change its type if it's already permissive). If TypeScript complains that `payload.visitorId` doesn't exist on the inferred type, widen by:
- If `payload` is typed `as FormLeadProgressInput` (from shared/schema), the visitorId field is already in the schema (Phase 4 plan 01) so this should compile.
- If `payload` is typed inline as a literal, change the assignment to `(payload as Record<string, unknown>).visitorId = storedVisitorId;` — but PREFER importing the proper type (`FormLeadProgressInput` or whatever the existing `payload` typing already is) and adding `visitorId` cleanly.

Read the actual `payload` declaration at the top of the function before deciding. The simplest correct path: `payload.visitorId = storedVisitorId;` and let TypeScript guide you.

Do NOT:
- Call `useAttribution()` in this component (per D-13).
- Read `localStorage.getItem('mvp_vid')` directly — use `getStoredVisitorId` so the key name lives in exactly one place (per Plan 01's MVP_VID_KEY rule).
- Block the form on visitor ID resolution. If `getStoredVisitorId()` returns null, the form must still submit normally (per D-14).
- Add `visitorId` to ANY error path — only the success path needs it for the server upsert.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from ['\"]@/lib/attribution['\"]" client/src/components/LeadFormModal.tsx` returns exactly 1 match.
    - `grep -n "getStoredVisitorId" client/src/components/LeadFormModal.tsx` returns exactly 2 matches (one import, one call).
    - `grep -n "payload.visitorId" client/src/components/LeadFormModal.tsx` returns at least 1 match.
    - The `getStoredVisitorId()` call appears BEFORE the `fetch("/api/form-leads/progress"` call: `grep -n "getStoredVisitorId\\|/api/form-leads/progress" client/src/components/LeadFormModal.tsx` shows getStoredVisitorId on a smaller line number than the fetch call.
    - `grep -n "useAttribution" client/src/components/LeadFormModal.tsx` returns NO matches (per D-13 — no hook call here).
    - `grep -nE "localStorage.getItem.*mvp_vid|'mvp_vid'|\"mvp_vid\"" client/src/components/LeadFormModal.tsx` returns NO matches (key name lives only in attribution.ts MVP_VID_KEY).
    - `npm run check` passes.
  </acceptance_criteria>
  <done>
    LeadFormModal's `payload` includes `visitorId` (when available) right before the POST to `/api/form-leads/progress`. The form gracefully submits without it when `mvp_vid` isn't yet in localStorage (e.g., user clears storage mid-session) — no UI blocking, no errors.
  </done>
</task>

</tasks>

<verification>
- `npm run check` passes after both tasks.
- `npm run dev` and visit `http://localhost:7000/?utm_source=google&utm_medium=cpc&utm_campaign=test`. Open DevTools → Network. Expect a POST to `/api/attribution/session` with body containing `ftSource: 'google'`, `ftMedium: 'cpc'`, `ftSourceChannel: 'Paid Ads'`. Expect another POST to `/api/analytics/hit` with `eventName: 'page_view'` and `visitorId: <uuid>`.
- Navigate to a different route. Expect a NEW `/api/attribution/session` POST (lt_* update) and a NEW `/api/analytics/hit` POST.
- Check `localStorage` in DevTools → Application → Local Storage. Key `mvp_vid` should be present with a UUID value. Refresh — the value persists.
- Open the lead form modal, fill in name, watch for POST to `/api/form-leads/progress`. Body should include `visitorId: <same-uuid>`.
- Verify in Supabase `visitor_sessions` table: a row with the new `visitor_id` UUID, `ft_source = 'google'`, `ft_medium = 'cpc'`, `ft_source_channel = 'Paid Ads'`, `ft_landing_page = '/'`. Verify `form_leads.visitor_id` is NOT NULL on the test row submitted from the form.
</verification>

<success_criteria>
1. `npm run check` passes.
2. `useAttribution` is called exactly once in App.tsx and zero times anywhere else in `client/src/`.
3. `reportAttributionPageView` fires from the `[location, visitorId]` effect — verified by network tab on every Wouter navigation.
4. Lead form submission (`/api/form-leads/progress`) includes `visitorId` in the body when `mvp_vid` is in localStorage.
5. After a real test submission, `form_leads.visitor_id` is populated for that lead in the database.
6. Hard refresh on the same browser keeps the same `mvp_vid` (SESSION-03).
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-utm-capture-hook/05-02-SUMMARY.md` documenting:
- Exact line numbers in App.tsx where the hook + page_view emit were inserted (for future reviewers).
- Exact line number where `payload.visitorId` was added in LeadFormModal.tsx.
- Any TypeScript/import quirks encountered.
- Confirmation that `mvp_vid` is now persisted across reloads (test result from running dev server).
</output>
