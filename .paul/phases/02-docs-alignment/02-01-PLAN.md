---
phase: 02-docs-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - AGENTS.md
autonomous: true
---

<objective>
## Goal
Rewrite the three top-level docs (`README.md`, `CLAUDE.md`, `AGENTS.md`) so they describe what this project actually is — the **production website for MVP (a service business) that also serves as a forkable template for other service-business sites** — and drop incorrect identifiers like "MVP Builder Remodeling" (remodeling business) and "marketing company" currently present.

## Purpose
Anyone landing in the repo (contributor, forker, future Claude session, teammate) should immediately understand the dual nature. Today, the docs mislead on business type and hide the forkability aspect that makes this project valuable beyond MVP itself.

## Output
- `README.md` rewritten: generic "service business website" tone with explicit "template + MVP production" framing, generic brand guideline section with per-fork override notes, tech stack stays accurate
- `CLAUDE.md` rewritten: project overview reflects dual nature; brand colors stay (they are MVP's live brand) but are marked as per-instance variables
- `AGENTS.md` extended with a short "Dual nature" note near the top and a pointer to `.paul/PROJECT.md` as the source of truth
- No other files touched — docs only
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md
@.paul/ROADMAP.md

## Source Files
@README.md
@CLAUDE.md
@AGENTS.md
</context>

<acceptance_criteria>

## AC-1: README.md reflects the dual nature accurately
```gherkin
Given the new README.md
When anyone reads the top paragraph
Then it states the project is the production website for MVP (a service business)
  AND that it is also a forkable template for other service-business sites
  And it does NOT mention "remodeling business" or "MVP Builder Remodeling" as the project identity
  And "Core Features" list matches the actual features (service catalog, booking, leads with scoring, admin dashboard, blog, gallery, FAQs, notifications, GHL sync, Supabase auth) — not the outdated list
  And "Brand Guidelines" (if present) are framed as "MVP's live brand — override per fork" rather than hardcoded project identity
  And Tech Stack section stays accurate (React 18, Vite, Express, Drizzle, Supabase, Vercel)
  And the Environment Variables section lists what's actually required today (DATABASE_URL, SESSION_SECRET, SUPABASE_URL/ANON/SERVICE_ROLE, ADMIN_EMAIL, ADMIN_PASSWORD_HASH) — not the obsolete list
```

## AC-2: CLAUDE.md reflects the dual nature and stays accurate for Claude guidance
```gherkin
Given the new CLAUDE.md
When Claude loads it at the start of a session
Then "Project Overview" section describes dual nature (production for MVP + forkable template)
  And "marketing company" and "[companyname]" placeholders are removed or replaced with an accurate description
  And Brand Guidelines are preserved but framed as "MVP's live brand (configurable per fork via admin)"
  And all development commands (`npm run dev` etc.) stay accurate with the correct port (7000 — not 5000)
  And Architecture and Key Patterns sections stay accurate for the current codebase
```

## AC-3: AGENTS.md acknowledges the dual nature
```gherkin
Given the new AGENTS.md
When a contributor reads it
Then a short section near the top explains the dual nature and points to `.paul/PROJECT.md` for authoritative context
  And existing sections (Project Structure, Build Commands, Coding Style, Testing, Commit, Security) stay accurate
  And the env var list is not duplicated (just reference README.md)
```

## AC-4: No regressions in code or tooling
```gherkin
Given only docs changed
When `npm run check` runs
Then TypeScript compiles with 0 errors (unchanged)
  And no code file was touched
  And the three docs have valid markdown (no broken headers, no unterminated code fences)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>README.md</files>
  <action>
    Rewrite `README.md` with:

    1. **Title**: `# MVP — Service Business Website Platform` (or similar — neutral). Drop "MVP Builder Remodeling".

    2. **Opening paragraph** (2-3 lines): State that this is (a) the production website for MVP — a service business — and (b) a reusable, forkable base for building websites for other service businesses. Mention the admin dashboard that lets a non-developer configure branding, SEO, and integrations without redeploy.

    3. **Core Features** (replace current list): Match what PROJECT.md says:
       - Public service catalog (categories, subcategories, cart, time-slot booking)
       - Lead capture form with scoring + classification (HOT/WARM/COLD) and abandonment SMS sweep
       - Admin dashboard (services, blog, gallery, reviews, FAQs, SEO, integrations, users, leads, notification audit log)
       - Multi-channel notifications (Twilio SMS, Resend email, GoHighLevel CRM sync) with full audit log
       - AI chat (OpenAI) with configurable agent + intake objectives
       - Responsive React UI with type-safe client/server contracts

    4. **Tech Stack**: Keep current content but verify accuracy. Current shows React 18, Vite, Wouter, TanStack Query, Tailwind/shadcn, Express, TypeScript, Drizzle, PostgreSQL, session-based auth with Supabase-backed identity. Drop the outdated line if it mentions Replit Auth anywhere.

    5. **Project Structure**: Keep accurate.

    6. **Environment Variables**: Update to match the actual `.env` shape used today. Required = DATABASE_URL, SESSION_SECRET, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ADMIN_EMAIL, ADMIN_PASSWORD_HASH. Do NOT list the admin password hash or any value; just the key name with an `=<value>` placeholder. Keep port note (default 7000 — matches `.env` today).

    7. **Getting Started / Scripts / Production / Security** sections: Keep as-is unless outdated.

    8. **New section at the end — "Using as a template"**:
       - One paragraph: how to fork, what to rebrand (company name in admin → Company Infos → Company Name; logos; brand colors via admin → Website → Colors; SEO metadata via admin → SEO), and a pointer to `.paul/PROJECT.md` for full context.

    Avoid:
    - Do NOT add marketing prose. Keep operator-focused.
    - Do NOT list license as anything other than MIT if the current file says MIT.
    - Do NOT hardcode brand colors in this file — refer to admin configuration instead.
    - Do NOT embed screenshots (deferred).
  </action>
  <verify>`grep -i "remodeling business" README.md` returns nothing; `grep -i "MVP Builder Remodeling" README.md` returns nothing; `grep "npm run dev" README.md` still returns at least one line; file opens without markdown parse errors</verify>
  <done>AC-1 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
    Rewrite `CLAUDE.md` in English (language memory applies):

    1. **Project Overview**: Rewrite the first paragraph. State: "This repository is the production website for MVP — a service business — and also serves as a forkable base template for building websites for other service businesses. Branding, SEO, and integrations are configurable via the admin dashboard with no redeploy."

       Remove the `[companyname]` placeholder AND the "marketing company" label. If the current file uses them, replace with "MVP" and "service business" respectively.

    2. **Commands**: Keep the full block but update `npm run dev` port comment to **7000** (match current `.env`). If 5000 is hardcoded anywhere else, replace.

    3. **Tech Stack**: Keep. Verify auth line says Supabase (not Replit). If Replit is mentioned, delete that line.

    4. **Architecture**: Keep. Double-check the `server/routes.ts` description — today it's modular via `server/routes/*.ts`, not a single 2800-line file. Update if wrong.

    5. **Key Patterns**: Keep. Type-Safe API, Shared Schema, Storage Layer, State Management — all still accurate.

    6. **Database Tables**: Add `notification_logs` to the list (shipped in v1.1 Phase 1).

    7. **Environment Variables**: Keep structure. Update to match README (SUPABASE_URL, ANON, SERVICE_ROLE added).

    8. **Brand Guidelines**: Reframe as **"MVP brand (this deployment) — configurable per fork via admin → Website → Colors and admin → Company Infos"**. Keep the actual color values listed (they're MVP's brand and useful for Claude to reference when reviewing changes), but make the header clear this is *MVP's* brand, not a project-wide contract.

    Avoid:
    - Do NOT remove the Brand Guidelines entirely — Claude uses them when assessing UI changes to the live MVP site.
    - Do NOT add instructions that contradict the existing memory (`MEMORY.md` says planning artifacts in English — `CLAUDE.md` itself is fine in English).
    - Do NOT list secrets or placeholder passwords.
  </action>
  <verify>`grep -i "marketing company" CLAUDE.md` returns nothing; `grep -c "companyname" CLAUDE.md` returns 0 (or a tagged mention explaining the fork override); `grep "Brand" CLAUDE.md` still finds the brand section</verify>
  <done>AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Extend AGENTS.md with a dual-nature note</name>
  <files>AGENTS.md</files>
  <action>
    Update `AGENTS.md`:

    1. Add a new **"About this repository"** (or similar header) section **near the top**, above "Project Structure & Module Organization":
       ```
       ## About This Repository

       This is the production website for MVP — a service business — and a forkable
       base template for building websites for other service businesses. See
       `.paul/PROJECT.md` for authoritative project context, milestones, and
       accumulated decisions.
       ```

    2. Keep all other sections unchanged — they are still accurate.

    3. If the **Environment Variables** subsection in "Security & Configuration" duplicates what README lists, compress it to a one-liner: `See README.md for the current `.env` contract.` Otherwise leave alone.

    4. Verify the port mention: if it says 5000, update to 7000.

    Avoid:
    - Do NOT rewrite the existing sections unless necessary for accuracy.
    - Do NOT add bullet lists of features — that belongs in README.
    - Do NOT add sections that duplicate content already in `.paul/PROJECT.md`.
  </action>
  <verify>`grep -c "About This Repository\\|About this repository\\|Dual nature" AGENTS.md` returns ≥1; `grep ".paul/PROJECT.md" AGENTS.md` returns at least one line</verify>
  <done>AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any code file (`.ts`, `.tsx`, `.sql`, `.json`, `.config.js`, etc.) — docs-only plan
- `.paul/**` — project planning artifacts are the source of truth, not edited here
- `docs/**` — other docs (DESIGN_SYSTEM.md, SEO_INJECTION_SYSTEM.md, ADMIN_REFACTOR_PLAN.md) stay as-is
- LICENSE, any other root-level docs not listed

## SCOPE LIMITS
- README.md + CLAUDE.md + AGENTS.md only
- No new docs files
- No screenshots, no badges, no marketing copy
- No changes to existing `docs/` folder
- No translations (docs stay in English per memory)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run check` → 0 errors (no code changed, should be trivial)
- [ ] `grep -i "remodeling business" README.md CLAUDE.md AGENTS.md` → no matches
- [ ] `grep -i "marketing company" README.md CLAUDE.md AGENTS.md` → no matches
- [ ] `grep "\\[companyname\\]" README.md CLAUDE.md AGENTS.md` → no matches
- [ ] README mentions "MVP" as the live deployment AND mentions "template" or "fork"
- [ ] CLAUDE.md mentions Supabase auth (not Replit)
- [ ] AGENTS.md points to `.paul/PROJECT.md`
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All three docs accurately describe the dual nature
- No code regressions (docs-only)
- New contributors / AI sessions can understand the project from the docs alone + .paul/PROJECT.md
- Milestone v1.1 ready to close after this plan's UNIFY + transition
</success_criteria>

<output>
After completion, create `.paul/phases/02-docs-alignment/02-01-SUMMARY.md`
</output>
